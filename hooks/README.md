# Hook scripts

These scripts are examples and are not enabled by default.

- `catastrophic_command_guard.py`: blocks a deliberately narrow set of catastrophic shell commands.

Run:

```bash
python3 hooks/catastrophic_command_guard.py --self-test
```

Review `docs/HOOKS.md` before activation.
