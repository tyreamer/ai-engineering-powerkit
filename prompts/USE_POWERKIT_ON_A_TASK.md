# Run This Task Through PowerKit

Apply `engineering-task-orchestrator` to the task below.

Use Prompt Preflight internally. Inspect the repository before asking questions. Silently resolve safe defaults from evidence. Ask only about unresolved material decisions.

Choose the lightest workflow that protects correctness. Use parallel read-only agents only for independent investigations and keep one writer by default.

Do not report completion until the appropriate verification loop, implementation critique, anti-slop review, and decision handoff are complete.

## Task

[Paste the outcome-oriented request here.]

## Known constraints

[Add only decisions or boundaries that are not already present in the repository.]

## Required handoff destination

[Task card, issue, pull request, report path, or “chat only.”]
